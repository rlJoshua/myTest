<nav class="navbar navbar-expand-lg navbar-light bg-dark">
    <a style="color: white;"class="navbar-brand" href="{{ path('home') }}">Portail</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText"
            aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto" >

            <li class="nav-item active">
                <a class="nav-link" style="color: white;" href="{{ path('list_member') }}">Membres</a>
            </li>

        {% if app.user %}
            <li class="nav-item active">
                <a class="nav-link" style="color: white;" href="{{ path('app_logout') }}">Déconnexion</a>
            </li>
        {% else %}
            <li class="nav-item active">
                <a class="nav-link" style="color: white;" href="{{ path('app_login') }}">Connexion</a>
            </li>
        {% endif %}

        </ul>
        <span style="color: white" class="navbar-text">
            {% if app.user %}
                {{ app.user.getLastName }} {{ app.user.getFirstName }}
            {% endif %}
        </span>
    </div>
</nav>
{% for message in app.flashes('notification') %}
    <div class="flash-notif">
        {{ message }}
    </div>
{% endfor %}